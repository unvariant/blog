# amateurs-ctf-2024

Authors writeups for `amateurs-ctf-2024`.

All the challenges should have at the minimum solves scripts at `exploit/solve.py`, and might have writeups if decide to stop being lazy.